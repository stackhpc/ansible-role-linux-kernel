---
# Install kernel headers
install_kernel_headers: true
install_perf: false
install_bpftool: false

# Kernel version to install (Default to any version)
kernel_version: ''

# Reboot if kernel was updated
reboot_on_kernel_update: true
